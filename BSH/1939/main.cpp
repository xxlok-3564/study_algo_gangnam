#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <queue>

using namespace std;

long long ans = 0;

void rec(long long left_ans, long long right_ans, )

    if(left > right)
        return;



int main() {

    int N; // 섬의 갯수
    int M; // 다리의 정보

    cin >> N >> M;

    int a, b;
    long long c;
    vector<int> arr[N];

    for (int i = 0; i < M; i++) {
        cin >> a >> b >> c;



    }

    int start_loc, target_loc;

    cin >> start_loc >> target_loc;



    return 0;
}